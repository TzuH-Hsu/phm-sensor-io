# CLAUDE.md

Read [AGENTS.md](AGENTS.md) first — it is the canonical instruction file for this repository. This file only adds Claude-specific notes.

- Run `make verify` before finishing any task that changed files.
- Load skills from `skills/` on demand only — never bulk-load.
- Never commit `*.local.md` files; never weaken lint configs to make checks pass.
