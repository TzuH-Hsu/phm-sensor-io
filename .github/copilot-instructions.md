# Copilot instructions

Read [AGENTS.md](../AGENTS.md) first — it is the canonical instruction file for this repository. This file only adds Copilot-specific notes.

- Follow the repository's existing conventions and patterns; prefer editing the Makefile over editing workflows.
- Load skills from `skills/` on demand only — never bulk-load.
- Never commit `*.local.md` files.
